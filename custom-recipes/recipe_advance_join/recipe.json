// This file is the descriptor for the Custom code recipe recipe_advance_join
{
    // Meta data for display purposes
    "meta" : {
        "label" : "Custom recipe from recipe recipe_advance_join",
        "description" : "Custom recipe",
        "icon" : "icon-puzzle-piece"
    },

    "kind" : "PYTHON",
    

    // Inputs and outputs are defined by roles. In the recipe's I/O tab, the user can associate one
    // or more dataset to each input and output role.

    // The "arity" field indicates whether the user can associate several datasets to the role ('NARY')
    // or at most one ('UNARY'). The "required" field indicates whether the user is allowed to
    // associate no dataset with the role.

    "inputRoles" : [
        {
            "name": "input_A_role",
            "label": "input A displayed name",
            "description": "what input A means",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true
            //,'mustBeSQL': true
        },
        {
            "name": "input_B_role",
            "label": "input B displayed name",
            "description": "what input B means",
            "arity": "NARY",
            "required": false,
            "acceptsDataset": true
            //,'mustBeSQL': true
            // ,'mustBeStrictlyType':'HDFS'
        }
        // ...
    ],

    "outputRoles" : [
        {
            "name": "left_output",
            "label": "left output displayed name",
            "description": "what left output means",
            "arity": "UNARY",
            "required": false,
            "acceptsDataset": true
        },
        {
            "name": "right_output",
            "label": "right output displayed name",
            "description": "what right output means",
            "arity": "UNARY",
            "required": false,
            "acceptsDataset": true
        },
        {
            "name": "inner_output",
            "label": "inner output displayed name",
            "description": "what inner output means",
            "arity": "UNARY",
            "required": false,
            "acceptsDataset": true
        }
        // ...
    ],

    // The field "params" holds a list of all the params
    // for wich the user will be prompted for values in the Settings tab of the recipe.

    // The available parameter types are:
    // STRING, INT, DOUBLE, BOOLEAN, PASSWORD, SELECT, MAP, COLUMN, COLUMNS, TEXTAREA

    "params": [
        {
            "name": "Join key for the left dataset",
            "type": "COLUMNS",
            "columnRole":"input_A_role"
        },
        {
            "name": "Join key for the right dataset",
            "type": "COLUMNS",
            "columnRole":"input_B_role"
        },
        {
            "name": "Operator",
            "label": "Operator",
            "type": "SELECT",
            "selectChoices": [
                {
                    "value": "AND",
                    "label": "Match when all the conditions are satisfied"
                },
                {
                    "value": "OR",
                    "label": "Match when one of the conditions are satisfied"
                }
        ]
        },
        {
            "name": "Columns from left dataset",
            "type": "COLUMNS",
            "columnRole":"input_A_role"
        },
        {
            "name": "Columns from right dataset",
            "type": "COLUMNS",
            "columnRole":"input_B_role"
        }

        // The 'COLUMNS' type works in the same way, except that it is a list of strings.
    ],

    // The field "resourceKeys" holds a list of keys that allows to limit the number
    // of concurrent executions and activities triggered by this recipe.
    //
    // Administrators can configure the limit per resource key in the Administration > Settings > Flow build
    // screen.

    "resourceKeys" : []

}
